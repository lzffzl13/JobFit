# Agent RAG Resume Matcher Development Progress

> 用途：记录本项目的开发进度、关键决策、验收结果和后续优化方向。换新对话或中断后，先读这个文件再继续。

## 当前状态

- 项目定位：面向 AI 应用/后端实习投递的简历-JD 匹配分析工具。
- 项目形态：FastAPI 后端 + 静态前端报告页 + DeepSeek 解释层 + 本地确定性评分引擎。
- 核心能力：输入简历和 JD，输出岗位匹配分、结构化 JD 要求、核心证据强度、岗位方向上限、匹配证据、差距、简历改写建议、面试问题和引用证据。
- 当前优化目标：已把评分逻辑从“扣分器”升级为分层评分引擎，避免靠单个坏例子反复打补丁。

## 已完成

- 搭建 FastAPI 后端服务和 `/jobfit/analyze` 分析接口。
- 支持粘贴简历文本，也支持 PDF/DOCX/TXT/Markdown 文件上传解析。
- 加入轻量 RAG 检索，用于召回简历和 JD 证据片段。
- 接入 DeepSeek Chat API，并保留未配置 API Key 或模型失败时的本地 fallback。
- 新增分层评分引擎：
  - `app/services/jd_parser.py`：JD 结构化、岗位类型和核心/加分项解析。
  - `app/services/resume_evidence.py`：简历证据抽取、技能证据强度、岗位相关性识别。
  - `app/services/scoring_policy.py`：核心分、JD 加分、额外竞争力和岗位方向上限。
  - `app/services/scoring.py`：组合评分结果并生成 API 输出。
- 重构 `app/services/llm.py`，使 DeepSeek 只负责解释、证据、建议和面试问题，不再决定最终分数。
- 重构 `app/services/jobfit.py`，在 DeepSeek 成功或失败后都用本地规则覆盖最终分数和结构化要求。
- 更新 `app/schemas/jobfit.py`，返回结构化 JD 要求、三段式分数和简历额外竞争力。
- 更新前端 `static/index.html`，改成上方输入、下方全宽分析报告。结果区展示总览、核心证据强度表、JD 结构化要求表、上限命中、评分备注、优化建议、面试问题和折叠引用证据。
- 补充验收测试，覆盖 JD 解析、二选一匹配、额外竞争力加分、fallback、LLM 分数覆盖等关键路径。
- 补充 README，说明新的三段式评分结构和接口返回字段。

## 关键设计

最终分数由本地确定性评分引擎计算：

```text
总分 = min(
    JD 核心匹配分 70
    + JD 加分项匹配分 15
    + 简历额外竞争力分 15,
    岗位方向上限
)
```

### JD 核心匹配

当前核心维度按 JD 动态提取，包括：

- Python/Java 基础
- Python Web 框架
- 接口开发能力
- MySQL/SQL
- Redis/缓存、数据结构/算法等会根据 JD 是否出现进入核心要求

每个核心项按证据强度计分：

- 0%：简历中完全没有出现该技能关键词。
- 30%：仅在技能栏/自评中出现，无项目或使用场景。
- 60%：学习、课程或 demo 级描述。
- 85%：有项目经验，但缺少具体技术细节。
- 100%：有项目 + 明确技术细节。

### JD 加分项

JD 有明确“加分项/优先/nice to have”等表达时，按命中率给满分 15。没有明确加分项时，按技术扩展广度给分，最高 10。

扩展技术包括：

- Docker / K8s
- Git
- Linux
- 自动化测试
- CI/CD
- 消息队列
- 监控

### 简历额外竞争力

当前额外竞争力最高 15 分，且仅限岗位相关技术证据：

- 技术量化成果：每项 1 分，最高 3 分。
- 技术竞赛/奖项：每项 2 分，最高 4 分。
- 开源贡献/技术博客：每项 2 分，最高 4 分。
- 复杂系统设计：每项 2 分，最高 6 分。
- 团队协作/文档规范：最高 2 分。

运营增长数据、非技术奖项、泛化协作经历不会进入技术岗位额外竞争力分。

### 岗位方向上限

当前硬上限包括：

- 无编程语言证据：最高 45。
- 无后端/Web/接口开发证据：最高 55。
- 无数据库/SQL 使用证据：最高 70。
- 核心技能项中 >=50% 为 0%：最高 75。
- 非技术简历且无技术项目：最高 55。
- 工作经历全部为非开发岗位且无后端项目：最高 65。
- 技能栏堆关键词但无项目证据：最高 70。

### JD 解析规则

已处理：

- `Python/JAVA`：视为二选一，命中 Python 或 Java 任一即可。
- `Django/Flask/FastAPI`：视为同类 Web 框架，命中任一即可。
- `列表、字典、元组等`：归为数据结构能力，不拆成多个独立要求。
- `查询、插入、更新、删除`：归为 SQL CRUD 能力，不拆成四个硬性技能。
- 中文顿号、英文逗号、括号、斜杠、换行粘连、`至少一种`、`任一` 等表达。

## 验收证据

当前通过的测试命令：

```powershell
.venv\Scripts\python.exe -m pytest -q
.venv\Scripts\ruff.exe check app tests
```

当前测试覆盖：

- 对用户给出的后端实习 JD，一份针对性简历可达到 90+。
- 中等匹配技术简历不会被抬到接近满分。
- 低匹配同方向简历会触发“无后端项目证据”等硬上限。
- 运营/市场/销售简历不会因为数据增长、活动、协作、非技术奖项拿到技术岗位高分。
- 技能栏堆关键词但无项目证据会被上限拦截。
- 奖项、项目、Docker、测试、部署会贡献简历额外竞争力分。
- `Django/Flask/FastAPI` 命中 FastAPI 即满足。
- `Python/JAVA` 命中 Python 即满足。
- `列表、字典、元组` 只归为数据结构。
- `查询、插入、更新、删除` 只归为 SQL CRUD。
- DeepSeek 失败时 fallback 仍能返回合理分数。
- DeepSeek 成功但返回异常低分时，最终分数仍由本地确定性规则覆盖。

当前手工冒烟样例：

- 中等 Flask 后端项目简历：约 77 分，不会被抬到 90+。
- 运营简历投后端 JD：约 0-26 分，触发无语言/无后端/无数据库等上限或低原始分。
- 用户实测两份技术向简历：约 78/80，低相关简历约 26，已经能拉开分布。

## 前端布局

当前页面：`static/index.html`

- 输入区放在页面上方，简历和 JD 双栏展示。
- 结果区独立放在输入区下方，不再挤在右侧窄栏。
- 顶部总览展示：
  - 总分
  - 原始分
  - 最终上限
  - 岗位核心匹配
  - JD 加分项
  - 简历竞争力
  - 模型状态
- 报告主体展示：
  - 上限命中
  - 待补强
  - 核心证据强度
  - JD 结构化要求
  - 已满足
  - 简历额外竞争力
  - 评分备注
  - 待补强建议
  - 简历优化建议
  - 面试问题
  - 引用证据，默认折叠

## 维护原则

- 不再针对单个异常样例做零散补丁。
- 新发现的问题应先归类成评分基准或策略缺口。
- 修改评分逻辑时同步补充 `tests/test_scoring_benchmark.py` 或相关测试，避免修一个例子破坏另一类简历。
- DeepSeek 输出只作为解释层参考，不能回写最终分数。

## 当前运行方式

本地开发：

```powershell
.venv\Scripts\activate
uvicorn app.main:app --reload --port 8077
```

如果 8077 端口被占用，可使用：

```powershell
uvicorn app.main:app --host 127.0.0.1 --port 8077
```

访问：

- Web 页面：http://127.0.0.1:8077
- API 文档：http://127.0.0.1:8077/docs

## 后续可选优化

- 为前端增加导出报告或复制 Markdown 报告功能。
- 增加更多岗位模板，如 Java 后端、AI 应用开发、LLM Agent 实习。
- 增加端到端前端截图或 Playwright 检查。
- 增加 CI 配置，自动运行 pytest 和 ruff。
